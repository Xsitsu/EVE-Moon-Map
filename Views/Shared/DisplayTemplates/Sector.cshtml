@model Sector 

<div class="container">
    <h3>@Model.Name</h3>
    <div>
        @{
            foreach (var item in Model.Planets)
            {
                Planet planet = item.Value;
                @Html.DisplayFor(model => planet);
            }
        }
    </div>
</div>
